import { getCloudflareContext } from "@opennextjs/cloudflare";
import Link from "next/link";
import { InternalNav } from "@/components/internal-nav";
import { listClients } from "@/lib/clients/repository";
import { listDomains } from "@/lib/domains/repository";
import { listWebsites } from "@/lib/websites/repository";
import { DomainCenter } from "@/components/dashboard/domain-center";
import { ContinueAction } from "@/components/dashboard/continue-action";
import { buildControlCenterRows, isLiveDomain } from "@/lib/dashboard/control-center";

export const dynamic = "force-dynamic";

export default async function DashboardPage() {
  const { env } = getCloudflareContext();
  const [clients, domains, websites] = await Promise.all([listClients(env.DB), listDomains(env.DB), listWebsites(env.DB)]);
  const rows = buildControlCenterRows(clients, domains, websites);
  const metrics = [["Total clients", clients.length], ["Websites ready", rows.filter((row) => row.website?.status === "ready_for_publication" || row.website?.status === "published").length], ["Live domains", domains.filter(isLiveDomain).length], ["Setup pending", rows.filter((row) => row.nextOperation !== "complete").length], ["Attention required", rows.filter((row) => row.attention).length]] as const;
  const readable = (value: string | null | undefined) => value ? value.replaceAll("_", " ") : "Not started";
  return <main className="min-h-screen bg-[#f6f8fc] px-5 py-8 text-slate-900 sm:px-8 lg:px-12 lg:py-10"><div className="mx-auto max-w-7xl"><header className="flex flex-col justify-between gap-5 border-b border-slate-200 pb-6 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-indigo-600">Owner control center</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-slate-950">Agentflow Client Provisioner</h1><p className="mt-2 text-sm text-slate-500">Operate websites, domains, DNS, Worker connections, and HTTPS from one workspace.</p></div><div className="flex flex-wrap items-center gap-2"><Link href="/" className="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-bold text-white">+ Create client</Link><Link href="/domains" className="rounded-xl border border-slate-300 bg-white px-4 py-2.5 text-sm font-bold text-slate-700">Search domain</Link><InternalNav /></div></header><section className="mt-7 grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{metrics.map(([label, value]) => <article key={label} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-wide text-slate-500">{label}</p><strong className="mt-3 block text-3xl text-slate-950">{value}</strong></article>)}</section><section className="mt-8 rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-slate-100 px-5 py-5"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-indigo-600">Operations</p><h2 className="mt-1 text-xl font-bold">Client provisioning queue</h2></div><Link href="/clients" className="text-sm font-bold text-indigo-700">View all clients</Link></div><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-slate-50 text-[11px] uppercase tracking-wide text-slate-500"><tr>{["Client", "Website", "Domain", "DNS", "HTTPS", "Setup", "Action"].map((label) => <th key={label} className="px-5 py-3">{label}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows.map((row) => <tr key={row.client.id} className="align-top"><td className="px-5 py-4"><Link href={`/clients/${row.client.id}`} className="font-bold text-slate-950 hover:text-indigo-700">{row.client.businessName}</Link><p className="mt-1 text-xs text-slate-500">{row.client.category}</p></td><td className="px-5 py-4 font-semibold capitalize">{row.website ? readable(row.website.status === "ready_for_publication" ? "ready" : row.website.status) : "Not generated"}</td><td className="px-5 py-4"><span className="font-semibold">{row.domain?.domain ?? "—"}</span>{row.domain ? <p className="mt-1 text-xs text-slate-500">Apex {readable(row.domain.customDomainStatus)} · www {readable(row.domain.wwwCustomDomainStatus)}</p> : null}</td><td className="px-5 py-4 font-semibold capitalize">{row.domain ? readable(row.domain.nameserverStatus === "configured" ? "active" : row.domain.nameserverStatus) : "—"}</td><td className="px-5 py-4 font-semibold capitalize">{row.domain ? readable(row.domain.sslStatus === "ready" ? "ready" : row.domain.sslStatus) : "—"}</td><td className="px-5 py-4"><span className={`font-semibold capitalize ${row.attention ? "text-amber-700" : "text-slate-700"}`}>{row.attention ? readable(row.attention) : readable(row.nextOperation === "complete" ? "complete" : row.nextOperation)}</span></td><td className="px-5 py-4"><ContinueAction clientId={row.client.id} operation={row.nextOperation} preferredDomain={row.client.domain} hasWebsite={Boolean(row.website)} websiteStatus={row.website?.status ?? ""} liveHostname={row.domain?.domain} /></td></tr>)}{rows.length === 0 ? <tr><td colSpan={7} className="px-5 py-12 text-center text-sm text-slate-500">No clients yet. Create your first client to start provisioning.</td></tr> : null}</tbody></table></div></section><DomainCenter clients={clients} /></div></main>;
}
